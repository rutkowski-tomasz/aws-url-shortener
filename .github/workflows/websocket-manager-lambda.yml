name: websocket-manager-lambda

on:
  push:
    branches:
      - "*"
    paths:
      - "src/websocket-manager-lambda/**"
  pull_request:
    branches:
      - "*"
    paths:
      - "src/websocket-manager-lambda/**"
  release:
    types: [published]

jobs:
  node:
    name: Node
    uses: ./.github/workflows/workflow-nodejs.yml
    with:
      project: websocket-manager-lambda
      node-version: 20.x
    secrets: inherit
