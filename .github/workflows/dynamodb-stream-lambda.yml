name: dynamodb-stream-lambda

on:
  push:
    branches:
      - "*"
    paths:
      - "src/dynamodb-stream-lambda/**"
  pull_request:
    branches:
      - "*"
    paths:
      - "src/dynamodb-stream-lambda/**"
  release:
    types: [published]

jobs:
  node:
    name: Node
    uses: ./.github/workflows/workflow-nodejs.yml
    with:
      project: dynamodb-stream-lambda
      node-version: 20.x
    secrets: inherit
