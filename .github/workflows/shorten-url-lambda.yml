name: shorten-url-lambda

on:
  push:
    branches:
      - "*"
    paths:
      - "src/shorten-url-lambda/**"
  pull_request:
    branches:
      - "*"
    paths:
      - "src/shorten-url-lambda/**"
  release:
    types: [published]

jobs:
  node:
    name: Node
    uses: ./.github/workflows/workflow-nodejs.yml
    with:
      project: shorten-url-lambda
      node-version: 20.x
    secrets: inherit
